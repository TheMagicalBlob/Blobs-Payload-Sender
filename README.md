TCP Payload sender. Supports sending elfdr payload loader before the selected payload (ps4 or ps5).
A dedicated port box for elfdr will be available if the option to send elfdr's been enabled

Has basic theme support. Saves user settings to the user's home folder
